export const G = 0.05; // gravitational constant
export const c = 300; // speed of light placeholder
export const SCALE = 1;
export const bodies = []; // global array to store all objects
export const scene = new THREE.Scene();
export const clock = new THREE.Clock();
